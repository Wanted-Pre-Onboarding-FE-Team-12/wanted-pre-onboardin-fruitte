import variables from '@styles/theme';

const ProductInfo = () => {
  return (
    <div>
      <h1 style={{ fontSize: variables.sideSpace.contentSmall }}>주문 상품 정보</h1>
      {/* <img src={productData[0].imgUrl} alt="" /> */}
    </div>
  );
};

export default ProductInfo;
